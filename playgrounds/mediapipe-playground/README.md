# MediaPipe Playground

This folder stores input and output data for the browser-side MediaPipe detect flow used by the shared Vision Lab.

The actual MediaPipe execution currently happens in the frontend so the lab can run detection locally in the browser while still keeping test data separated under `playgrounds/`.
